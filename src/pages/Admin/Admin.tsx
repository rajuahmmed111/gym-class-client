import React from 'react';

const Admin = () => {
    return (
        <div>
            <button className='btn btn-success' >add Trainer</button>
            <button className=' btn btn-accent'>Schedule</button>
        </div>
    );
};

export default Admin;